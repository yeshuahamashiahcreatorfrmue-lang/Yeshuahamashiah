"""Yeshuahamashiah Engine.

A small RPG Maker (쯔꾸르) style 2D game engine built on pygame.

Features
--------
- Tile based maps with multiple layers, collision and events.
- Asset registration system (tilesets, characters, audio).
- A built-in map editor and a game runtime in one application.
- Switch between editing and playing at runtime with a single key.

The whole thing is packaged into a single executable so that double
clicking the .exe launches straight into the engine.
"""

__version__ = "0.1.0"
