# 윈도우 동작 점검 체크리스트 + 보완/최적화 (빌드 0627d)

실제 Windows(windows-latest, MSVC) CI(`.github/workflows/windows-build.yml`)로
빌드·스모크하고, 헤드리스 셀프테스트/스모크로 데이터·로직을 검증한 결과.

## A. 빌드/실행 (Windows 실기)
- [x] MSVC Release 빌드 성공 (OpenGL 2.1, AI 컷아웃 ON) — CI 53c04f5 success
- [x] GPU 없는 러너에서도 Mesa 소프트웨어 GL 번들로 정상 기동(스모크 통과)
- [x] 한글 경로/계정명 안전(CP_ACP 변환), 파일 복사 Unicode-safe(CopyFileW)
- [x] 크래시 핸들러(SetUnhandledExceptionFilter) + 로그 다중 경로 기록

## B. 에디터 탭 (11개 — 전부 구현·배선)
- [x] 월드 / 전맵뷰어(Ctrl+휠 줌·맵 배치·복붙) / 맵(3레이어+충돌·5도구·undo)
- [x] NPC(검색·진영/AI·초상·외부 이미지) / 이벤트(8종·3트리거·조건)
- [x] 캐릭터·몹(6모션·스킬·발판) / 대화(맵 NPC 편집·여러 NPC·초상)
- [x] 시나리오(맵 위 위치·반경·복수 제거) / 에셋(드래그·GIF) / DB(아이템·액터·적)
- [x] 모든 목록: 검색 + 웹 스크롤바 + 중클릭 오토스크롤
- [x] 모든 선택 UI: 클릭→전체 펼침→선택(드롭다운). 사이클 버튼 잔존 0

## C. 런타임 (7 페이즈)
- [x] Field: 이동·충돌·근접/스킬(Z X C V F G)·쿨다운·MP·몹 AI·투사체·데미지 표시
- [x] Message / Menu(인벤·장비·스탯·세이브) / Shop(구매·판매)
- [x] GameOver / GameClear / Dialogue(분기·대답·대응·초상)
- [x] 생존(허기/목마름/버프), 레벨업, 퀘스트, 세이브/로드(F9/F12·자동)

## D. 데이터 직렬화 라운드트립 (셀프테스트로 검증)
- [x] 아이템/액터/적/스킬/캐릭터/몹
- [x] 대화: 라인·**speakerAsset(여러 NPC 초상)**·대답·대응 7종
- [x] 시나리오: 동작 6종·**editMapId**·**이펙트 radius**·**제거 removeTags(복수)**
- [x] 맵: 타일3레이어+충돌·이벤트·몹스폰·인카운터·BGM·날씨·암흑·낮밤·애니타일
- [x] 게임상태: 인벤·파티·장비·버프·퀘스트·스위치/변수·위치·플레이시간
- [x] 추가/삭제 잔여물 없음(맵·에셋 200회 사이클·참조 스크럽)
- [x] 셀프테스트 전체 PASS(신규 항목 5개 포함)

## E. 이번 보완 기능
- [x] 대화 대답을 **키보드 1~4**로 선택(클릭과 병행)
- [x] **컷신 ESC 스킵** + 화면 우하단 "컷신 진행중 · ESC" 표시(대기/이동 루프에 갇히지 않음)
- [x] 대화 라인마다 말하는 NPC가 다르면 **초상도 전환**(런타임 말풍선 왼쪽)

## F. 최적화 / 리팩토링
- [x] **컴파일 경고 0** (이전: 미스리딩 들여쓰기 8 · 미초기화 6 · strict-aliasing 3 · 미사용 1)
- [x] 드롭다운 픽커를 `int*`/`string*` 2분기 → **단일 `std::function` 콜백**으로 통합
      (`pickerApply_`), enum은 `optionButtonEnum<>`로 **reinterpret_cast 제거**(UB 해소)
- [x] 한 줄 다중 `if` 분리, 미사용 람다(cycleImg) 제거, 집합초기화 신규 필드 채움
- [x] `e.text`로 대화 생성 시 speakerAsset를 0이 아니라 실제 그래픽/-1로 설정(버그 수정)

## 헤드리스로는 눈으로 못 보는 항목(코드상 구현 확인됨)
날씨 파티클·암흑 토치광·낮밤 틴트·애니타일 토글·존 경계 전환·한글 IME 실시간 조합
(IME는 Windows 전용 — 실기 확인 필요). 모두 구현되어 있고 빌드/스모크는 통과.
