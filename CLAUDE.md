# 프로젝트 규칙 (모든 Claude 인스턴스 필수 준수)

## ★ 채팅 원문 영구 보존 — 반드시 지킬 것
이 저장소는 **사용자의 모든 채팅 메시지를 원문 그대로** 보존한다. /clear·세션 리셋·새 인스턴스여도 예외 없다.

- 원천: `docs/요청기록.json` / 가독본: `docs/요청기록_원문.md`
- 갱신기: `scripts/save_chat_log.js` — 세션 트랜스크립트(`~/.claude/projects/.../*.jsonl`)에서 사용자 메시지를 뽑아 기존 기록과 **병합**(중복 제거)해 저장. 병합이라 기존 기록이 절대 사라지지 않는다.
- 자동 실행: `.claude/settings.json`의 **SessionStart + UserPromptSubmit** 훅이 매번 호출.

### 새 인스턴스가 할 일
1. 세션 시작 시 훅이 자동으로 `save_chat_log.js`를 돌려 기록을 최신화한다(수동 확인: `node scripts/save_chat_log.js`).
2. 사용자가 "내가 말한 것/채팅 목록"을 요구하면 **요약에 의존하지 말고** `docs/요청기록_원문.md`(없으면 위 스크립트 실행)에서 **원문 그대로** 제시한다.
3. 이 기록 파일들은 **삭제·축약 금지.** 컨텍스트 요약과 무관하게 항상 원문이 기준이다.
4. 변경 커밋 시 갱신된 `docs/요청기록*`도 함께 커밋해 원격에 보존한다.

## 프로젝트 개요
- 단일 파일 게임: `index.html` (캔버스 + 바닐라 JS). 모바일 세로형 월드맵 RTS+RPG.
- 설계 기획서: `docs/기획서/` (카테고리별, 00_연혁에 전체 결정 시간순).
- 아이디어는 **임의 폐기 금지** — 변경은 사용자 지시가 있을 때만, 이전 안도 보존.
